# Quantify the lag effect explicitly
by_month = incidents_joined.assign(month=incidents_joined["opened"].dt.to_period("M"))
match_by_month = by_month.groupby("month")["technology"].apply(lambda s: s.notna().mean())
print(match_by_month)
