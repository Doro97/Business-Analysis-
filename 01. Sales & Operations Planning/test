
python3 -c "
from policy_twin.adapters.db import sync_conn
from policy_twin.adapters.graph import age_setup_sync, cypher_sync

with sync_conn() as conn:
    age_setup_sync(conn)
    rows = cypher_sync(
        conn,
        'MATCH ()-[r]->() RETURN DISTINCT label(r) AS edge_type, count(*) AS n',
        ['edge_type', 'n'],
        graph='scenario_36a61c5b8abb2a10f07db5665a7aa979be0b462ed73f21a6a3db69056be9564f',
    )
    for r in rows:
        print(r)
"
