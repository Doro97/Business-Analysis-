spike_weeks = incidents_joined[
    (incidents_joined["week_start"] >= "2026-04-20") &
    (incidents_joined["week_start"] <= "2026-05-18")
]
print(spike_weeks.groupby(spike_weeks["opened"].dt.date).size().sort_values(ascending=False).head(10))
