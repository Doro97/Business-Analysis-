python3 -c "
from policy_twin.adapters.db import sync_conn
with sync_conn() as conn:
    rows = conn.execute(
        \"SELECT DISTINCT doc_id FROM text_chunk WHERE doc_id LIKE %s\",
        ('%941240753a%',)
    ).fetchall()
    for r in rows:
        print(r)
"
