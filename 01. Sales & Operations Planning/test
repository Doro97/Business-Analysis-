unmatched = incidents_joined[incidents_joined["technology"].isna()]
print(unmatched.shape)

# Is it random, or concentrated somewhere? Check a few angles:
print(unmatched["state"].value_counts(normalize=True))
print(unmatched["business_service"].value_counts(normalize=True).head())
print(unmatched["opened"].dt.to_period("M").value_counts().sort_index())
